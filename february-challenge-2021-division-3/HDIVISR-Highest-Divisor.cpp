#include <bits/stdc++.h>
#include <cmath>
using namespace std;
int main(){
    int n; cin>>n;
    int max_div = 1;
    for(int d=0; d<sqrt(n); d++){
        if(n%d==0 && d>=max_div)
    }
    return 0;
}